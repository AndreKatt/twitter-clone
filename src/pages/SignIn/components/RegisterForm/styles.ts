import { styled } from "@mui/material";
import Alert from "@mui/material/Alert";
// types
import type { AlertProps } from "@mui/material/Alert";

export const SignAlert = styled(Alert)<AlertProps>({
  width: "100%",
});
