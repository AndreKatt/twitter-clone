import { styled } from "@mui/system";

export const PageSpinnerWrapper = styled("div")({
  textAlign: "center",
  paddingTop: 150,
});

export const ElementSpinnerWrapper = styled("div")({
  textAlign: "center",
  paddingTop: 50,
  paddingBottom: 50,
});
