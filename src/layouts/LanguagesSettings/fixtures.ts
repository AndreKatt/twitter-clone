import type { GetButtonLabels } from "./types";

export const getButtonLabels: GetButtonLabels = (t) => ({
  russian: `${t("layouts.languagesSettings.buttonLabels.russian")}`,
  english: `${t("layouts.languagesSettings.buttonLabels.english")}`,
});
