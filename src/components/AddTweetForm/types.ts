export type AddTweetFormProps = {
  maxRows?: number;
  minHeight: number;
  divider?: boolean;
};

export type StyledCircularProgressProps = {
  textLength: boolean;
};

export type BodyContainerProps = {
  minHeight: number;
};
